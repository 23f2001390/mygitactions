name: Test Action

on:
  push:
    branches:
      - main  # or any branch you want to trigger the action

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print Email and Hello World
        run: |
          echo "23f2001390@ds.study.iitm.ac.in"
          echo "Hello, world!"
